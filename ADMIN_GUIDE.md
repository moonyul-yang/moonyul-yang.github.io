# 홈페이지 관리 안내

## 접속

- 관리자: https://app.pagescms.org/
- 저장소: `moonyul-yang/moonyul-yang.github.io`
- 편집 브랜치: `main`
- 홈페이지: https://moonyul-yang.github.io/
- 반영 상태: https://github.com/moonyul-yang/moonyul-yang.github.io/actions

Pages CMS에 로그인하고 위 저장소와 `main`을 선택하세요. 처음 연결했던 화면이 남아 있으면 새로고침하세요.

## 수정·추가·삭제

1. 왼쪽에서 기본 소개, 연구 주제, Research Projects, 경력, Honors, Selected Work, 학력 중 원하는 메뉴를 엽니다.
2. 기존 항목을 펼쳐 내용을 수정하거나 항목 목록의 추가 버튼으로 새 항목을 만듭니다.
3. 표시 순서는 작은 숫자가 위로 갑니다. 기존 항목은 10, 20, 30 순서이므로 중간에 넣을 때는 15와 같은 숫자를 사용할 수 있습니다.
4. 새 항목은 기본적으로 홈페이지 표시가 꺼져 있습니다. 공개할 준비가 되면 **홈페이지에 표시**를 켜세요.
5. 저장(Save)을 누릅니다. GitHub가 자동으로 홈페이지를 다시 만들어 배포합니다.
6. Actions에서 최근 실행의 성공을 확인하고 홈페이지를 새로고침하세요. 반영에는 보통 몇 분이 걸립니다.

항목 목록에서 제거한 뒤 저장하면 홈페이지에서도 삭제됩니다. 잠시 숨기려면 삭제 대신 홈페이지 표시를 끄세요. 같은 기간의 여러 직책은 경력 한 항목 안의 직책 목록으로 관리합니다.

## 사진과 로고

- 프로필 사진: 기본 소개·사진 메뉴에서 교체합니다.
- 기관 로고: 기관 로고 메뉴에서 해당 기관의 이미지를 교체합니다.
- 연구과제·학력·수상: 각 항목의 기관 로고 선택란에서 사용할 로고를 선택합니다.
- 이미지는 `assets/images`에 저장됩니다. 기존 로고는 파일명을 그대로 덮어쓰기보다 새 이미지로 올린 뒤 선택하는 편이 캐시 문제를 줄입니다.
- 본문의 KAIST는 현재 결합 로고의 파란 KAIST 부분만 표시하도록 설정되어 있습니다. 이 항목을 교체할 때는 같은 비율의 KAIST STP 결합 이미지를 사용하세요. 독립 KAIST 이미지로 변경하려면 표시 스타일도 함께 수정해야 합니다.
- 현재 등록되지 않은 새 기관을 추가하려면 관리자 설정의 로고 목록도 확장해야 합니다.
- 사용 중인 이미지를 미디어에서 먼저 삭제하면 홈페이지에 깨진 이미지가 생깁니다. 해당 항목의 이미지를 교체하거나 항목을 삭제한 뒤 미디어 파일을 정리하세요.

## 공개 범위 주의

이 GitHub 저장소는 공개입니다. **홈페이지 표시를 끄는 것은 비공개 보관이 아닙니다.** 숨긴 글, 삭제 전 기록, 업로드한 파일은 저장소와 변경 이력에서 보일 수 있습니다. 비공개 연구자료, 군 관련 비공개 정보, 미완성 CV는 업로드하지 마세요.

CV 다운로드 기능은 제공하지 않습니다. 기존 CV 파일은 홈페이지 배포에서 제외했습니다. 다만 과거에 저장소에 올라간 CV와 그 이력은 별도로 정리하지 않는 한 GitHub에서 공개될 수 있습니다.

문서 링크는 이미 공개된 `https://` 주소를 사용하세요. 컴퓨터 안의 `file:///C:/...` 주소는 다른 방문자가 열 수 없고 입력도 허용하지 않습니다.

## 반영이 되지 않을 때

1. 저장 버튼을 눌렀는지, `main`을 편집했는지, 항목의 홈페이지 표시가 켜져 있는지 확인합니다.
2. Actions에서 최근 실행이 완료되었는지 확인합니다. 실패한 경우 이전 정상 홈페이지가 유지됩니다.
3. 컴퓨터에서 Ctrl+F5로 새로고침하거나 다른 브라우저에서 확인합니다.
4. 문제가 계속되면 변경한 메뉴와 실패한 Actions 주소를 전달해 주세요.

## 복구와 유지보수

- GitHub의 각 데이터 파일 History에서 이전 내용을 확인하고 복구할 수 있습니다. 일반적인 수정은 관리자 화면에서 되돌려 저장하는 것이 가장 쉽습니다.
- 페이지 디자인은 `index.html`, `_includes`, `styles.css`, `enhancements.css`가 담당합니다. 글을 바꾸기 위해 이 파일들을 편집하지 마세요.
- 편집 데이터는 `_data/*.json`, 관리자 양식은 `.pages.yml`에 있습니다.
- GitHub Pages의 게시 소스는 `main` 브랜치의 루트 폴더를 유지합니다. 기본 Jekyll 배포를 사용하므로 별도 서버, API 키, 사용자 지정 배포 작업은 필요하지 않습니다.
- `.nojekyll` 파일을 다시 만들면 데이터가 홈페이지로 변환되지 않습니다.
- 배포 제외는 `_config.yml`의 `exclude`로 관리합니다. 이는 저장소의 공개 여부를 바꾸지 않습니다.
- Pages CMS 연결은 이 홈페이지 저장소 하나로 제한하고 GitHub 계정에 2단계 인증을 설정하는 것을 권장합니다.
